/**
 * Stage: media_plan — Mazhir-grade campaigns, ad groups, budgets, KPIs,
 * bid-strategy gates. Existing logic in services/mazhirMediaPlan.ts;
 * Phase 3 wraps it so the media plan surfaces in the unified pipeline
 * status alongside other stages.
 */

import type { Context } from 'hono'
import { notImplementedYet } from './_stub'

export async function run(c: Context): Promise<Response> {
    return notImplementedYet(c, 'media_plan')
}